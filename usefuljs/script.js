const matrix = Array(5)
  .fill("what")
  .map(() => Array(5).fill("hossam"));

console.log(matrix);
console.log(matrix.length);

const array = [5, 4, 7, 8, 9, 2];

console.log(array.reduce((a, b) => a + b));
